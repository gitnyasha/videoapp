<p>
  <strong>Title:</strong>
  <%= @image.title %>
</p>
<p>
  <strong>File:</strong>
  <%= image_tag @image.file.url if @image.file? %>
</p>
<%= link_to 'Edit', edit_image_path(@image) %> |
<%= link_to 'Back', images_path %>
